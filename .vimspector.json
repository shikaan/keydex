{
  "configurations": {
    "run": {
      "adapter": "delve",
      "variables": {
        "dlvFlags": "--check-go-version=false"
      },
      "configuration": {
        "request": "attach",
        "program": "${workspaceFolder}",
        "processId": 0
      }
    }
  }
}
